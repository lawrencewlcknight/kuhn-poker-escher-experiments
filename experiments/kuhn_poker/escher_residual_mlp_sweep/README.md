# ESCHER Residual-MLP Sweep

Single-seed diagnostic sweep comparing plain, same-width residual, and projection residual MLPs.

```bash
python -m experiments.kuhn_poker.escher_residual_mlp_sweep.run
```

