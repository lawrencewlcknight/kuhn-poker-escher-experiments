# ESCHER Policy-Network Width Sweep

Single-seed diagnostic sweep that isolates average-policy-network capacity while holding regret and value networks at the Experiment 13 reference size.

```bash
python -m experiments.kuhn_poker.escher_policy_network_width_sweep.run
```

