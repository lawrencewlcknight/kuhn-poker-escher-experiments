# ESCHER Shared-Trunk/Action-Head Sweep

Single-seed diagnostic sweep comparing the current shared trunk plus linear action-output layer with separate shallow per-action heads for the policy and regret networks.

```bash
python -m experiments.kuhn_poker.escher_shared_trunk_head_sweep.run
```

